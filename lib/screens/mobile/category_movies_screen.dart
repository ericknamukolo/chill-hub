import 'package:flutter/material.dart';

class CategoryMovieScreen extends StatelessWidget {
  const CategoryMovieScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
